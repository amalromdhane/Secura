🧩 STEP 1 — Create the Database
Open phpMyAdmin (from XAMPP or browser: http://localhost/phpmyadmin)
Click New
Create a database:
cyberaware
🧩 STEP 2 — Create Tables
👤 Users table
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    email VARCHAR(100),
    password VARCHAR(255)
);